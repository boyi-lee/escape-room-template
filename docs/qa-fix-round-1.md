# 第一輪修正驗證

日期：2026-07-30
分支：`feat/tetrabiblos-learning-site`
PR：#1

## 已修正

1. 原典可回查
   - 16 個核心章節皆增加 `Book` 與 `Chapter` 定位。
   - 內容明確標示為教學摘要，不冒充逐字翻譯。

2. 真正的閱讀後解鎖
   - 每章加入章節檢核。
   - 答對後才寫入通過紀錄並解鎖下一章。
   - 解鎖流程會跨越四書，完成第四書末章後顯示核心章節完成。

3. 章節級搜尋
   - 搜尋索引涵蓋書名、章名、原典定位、摘要、白話與規則。
   - 搜尋結果直接顯示對應章節。
   - 未解鎖結果保留可見，但禁止跳過學習順序。

4. 程式可維護性
   - `index.html`：語意結構與資源載入。
   - `assets/css/styles.css`：視覺與 RWD。
   - `assets/js/data.js`：知識與題目資料。
   - `assets/js/app.js`：互動、搜尋、解鎖與儲存。

5. 學習紀錄容錯
   - `localStorage` 解析加入 `try/catch`。
   - 異常資料會回復安全預設值，不再使整頁停止。

6. README 精確化
   - 改為無框架、無資料庫、無後端執行依賴。
   - 明確標示 Google Fonts 是唯一外部前端資源。

## 程式級驗證

- 分支相對 `main`：ahead 8、behind 0。
- 主要程式均為格式化的多行檔案。
- 解鎖鍵格式固定為 `bookIndex-chapterIndex`。
- 初始只解鎖 `0-0`。
- 錯誤答案不會寫入進度；正確答案才保存通過與下一章解鎖。
- 重置會清除 V2 學習紀錄並回到第一章。

## 尚待執行

- GitHub Pages 預覽環境驗收。
- iPhone Safari 真機操作。
- 桌機 Chrome／Safari 基本瀏覽。
- 字體離線降級外觀確認。

在完成瀏覽器與真機驗收前，PR 維持不合併。
